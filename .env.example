# Local environment template — copy to .env (gitignored) and fill in.
# .env is loaded automatically on package import (see screener/config.py).

# Groq API key — free tier at https://console.groq.com/keys
GROQ_API_KEY=gsk_your_key_here
