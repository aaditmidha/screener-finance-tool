# Development & test dependencies — install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==9.0.3
pytest-cov==7.1.0

# Browser fallback for blocked scrapes (heavy — not deployed to Streamlit Cloud;
# run `playwright install chromium` after installing)
playwright==1.60.0

# NOTE: pdfplumber moved to runtime requirements.txt — the upload-driven
# extraction runs on the hosted app, not just locally.
